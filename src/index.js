import React, { StrictMode } from "react";
import ReactDOM from "react-dom";
import App from "./App";
import "./index.css";

let id = document.getElementById("root");
ReactDOM.render(
  <StrictMode>
    <App></App>
  </StrictMode>,

  id
);
